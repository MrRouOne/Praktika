using namespace std;
#include <fstream>
#include <iostream>
#include <string>
using namespace std;

int main()
{

	setlocale(LC_ALL, "Russian");
	ofstream fout("fist.txt");
	struct zavod {
		char fio[40];
		char gorod[5];
		int sum_ball;
	} mstud[100];
	int i, k;

	cout << "введите количество учеников" << endl;
	cin >> k;

	for (i = 0; i < k; i++) {
		cout << "введите фио " << endl;
		cin >> mstud[i].fio;

		cout << "введите город " << endl;
		cin >> mstud[i].gorod;

		cout << "введите суммарный балл" << endl;
		cin >> mstud[i].sum_ball;

	}
	for (i = 0; i < k; i++) {
		if (mstud[i].sum_ball > 220 && strcmp(mstud[i].gorod, "Minsk") == 0)

		{
			fout << mstud[i].fio << endl;
			cout << mstud[i].fio << endl;
		}


	}
	fout.close();
}

